cslm:
  autoOpenBrowser: true
  shiro:
    # token有效期，单位秒
    jwtTimeOut: 3600
    # 后端免认证接口 url
    anon_url: /login,/logout/**,/regist,/user/check/**,/swagger-resources/**,/webjars/**,/v2/**,/swagger-ui.html/**,/favicon.ico
  maxBatchInsertNum: 300
